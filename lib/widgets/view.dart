
import 'package:flutter/widgets.dart';

class ViewArticle extends StatelessWidget {
  const ViewArticle({super.key});

  @override
  Widget build(context) {
    return const Placeholder(fallbackHeight: 1);
  }
}